# ============================
#  SkinMatch Frontend ENV File
#  1) Copy to: frontend/.env.local
#  2) Fill values inside <...> 
# ============================


# --- App / Dev Mode ---
NEXT_PUBLIC_USE_MOCK  0

# --- Google OAuth (Frontend) ---
# Google Client ID from Google Cloud Console
NEXT_PUBLIC_GOOGLE_CLIENT_ID=<CHANGE_ME_GOOGLE_CLIENT_ID>

# --- Public Backend URL (Browser will call this) ---
# Local dev example:
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# API prefix exposed to browser
NEXT_PUBLIC_API_BASE=/api/v1

# --- Internal URLs (Docker Network / Server-side) ---
API_BASE=http://backend:8000/api
BACKEND_URL=http://backend:8000
